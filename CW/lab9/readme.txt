the advantage of vector is the operation is O(1), while the map is O(log(n)).

the disvantage of vector is the initialization. It takes O(N) to generate and take the space
as the map don't need to do that.

So map has better initialization but vector has better operation.